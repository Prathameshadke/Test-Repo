here we are going to see some more git commands starting with
git diff shows the exact changes you made in your files that are not yet saved (committed).
git diff --stat shows a summary of what changed in your files.
sample2.txt → The file that was changed
2 → Total number of lines affected
+ → One line was added
- → One line was removed
1 file changed Only one file was modified
1 insertion(+)One new line was added
1 deletion(-)One old line was removed
git diff → shows full details
git diff --stat → shows summary only
git diff --staged shows the changes that are already added and will be included in the next commit.

git log --oneline shows the commit history in a short and clean format.
git log --graph Shows commit history in a visual tree format. It shows how different commits and branches are connected.

git commit --amend?
Used to edit or fix the last commit.
Only do this before pushing.
Change the commit message
Add missing files

.gitignore
"Do NOT track these files."
Why it is important:
Keeps secrets safe
Avoids junk files
Keeps repo clean
.gitignore is a blacklist of files Git should ignore. we add the file name in .gitignore to ignore that file.q